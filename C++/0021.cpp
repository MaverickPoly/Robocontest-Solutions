#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c; cin >> a >> b >> c;
    
    int result = (a + 1) / 2 + (b + 1) / 2 + (c + 1) / 2;
    cout << result << endl;

    return 0;
}
